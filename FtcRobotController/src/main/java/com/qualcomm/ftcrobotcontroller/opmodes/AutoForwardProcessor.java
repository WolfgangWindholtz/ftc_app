package com.qualcomm.ftcrobotcontroller.opmodes;




public class AutoForwardProcessor {
    /*public AutoForwardCommand consume(AutoForwardSample sample) {
        motor1.setPower(sample.power);
        motor2.setPower(sample.power);
        motor3.setPower(sample.power);
        MapLoader.motor4.setPower(sample.power);
    }/*/
}
